# Label Studio Runtime Configuration
# -----------------------------------------------------------------------
# LABEL_STUDIO_LOCAL_FILES_SERVING_ENABLED:
#   Allows Label Studio to serve images directly from the mounted host
#   directory instead of requiring a full upload to the database.
#
# LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT:
#   Must match the mount target in docker-compose.yaml. All image paths
#   in generated JSON tasks are relative to this root.
# -----------------------------------------------------------------------
LABEL_STUDIO_LOCAL_FILES_SERVING_ENABLED=true
LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT=/label-studio/files

# Bootstrap account (local research template values)
LABEL_STUDIO_DISABLE_SIGNUP_WITHOUT_LINK=true
LABEL_STUDIO_USERNAME=admin@berana.local
LABEL_STUDIO_PASSWORD=change_me_local_only

# Use lightweight SQLite
DJANGO_DB=sqlite

# setup.sh will replace these with host values
HOST_UID=1000
HOST_GID=1000
